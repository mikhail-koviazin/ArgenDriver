/**
 * Native stub. Vercel Web Analytics counts visits to the web deployment, so there is nothing
 * for it to do in the Android and iOS builds; the real implementation is `webAnalytics.web.ts`.
 */
export function initWebAnalytics() {
  // Intentionally empty.
}
